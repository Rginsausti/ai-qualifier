FROM ubuntu:22.04
RUN apt-get update && apt-get install -y build-essential cmake git wget curl python3 python3-pip
WORKDIR /opt/llama
# Placeholder: instructions to build llama.cpp or a chosen runtime.
# This Dockerfile is a starter. Building full llama/ggml requires more steps and large model files.

CMD ["/bin/bash"]
